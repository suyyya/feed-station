# 🍳 投喂站 · 部署到 Zeabur 保姆级指南

> 目标：让朋友通过一个网址就能打开你的投喂站，随时随地互喂。
> 全程免费、不用绑卡，大约 20 分钟。**你只需要一个 GitHub 账号**（没有的话先花 2 分钟注册一个：https://github.com/signup）。

---

## 你的项目里已经配好的东西

| 文件 | 作用 |
|---|---|
| `package.json` | 告诉 Zeabur 这是 Node 项目，用 `npm start` 启动 |
| `server.js` | 已支持 `PORT` / `DATA_DIR` / `UPLOAD_DIR` 环境变量 |
| `.gitignore` | 本地数据目录不入库，云上数据靠持久卷 |

---

## 第一步：把代码放到 GitHub

### 方式 A：网页上传（推荐，不用命令行）

1. 打开 https://github.com/new ，创建一个新仓库：
   - **Repository name**：`feed-station`
   - 选 **Public**（公开，部署用）或 **Private**（私有，都行）
   - 不要勾选 "Add a README" 等初始化选项（保持空仓库）
   - 点 **Create repository**
2. 在新仓库页面点 **uploading an existing file**（上传现有文件）
3. 把 `feed-station` 文件夹**拖进网页**（文件夹结构会自动保留）：
   - `server.js`
   - `package.json`
   - `public/index.html`（注意要保留 `public` 文件夹）
   - `test-api.sh`（可选）
   - `.gitignore` 是隐藏文件，网页拖拽可能看不到，**跳过它没关系**，不影响部署
4. 点 **Commit changes** → 提交完成 ✅

### 方式 B：命令行（如果你会 git）

```bash
cd feed-station
git remote add origin https://github.com/<你的GitHub用户名>/feed-station.git
git push -u origin main
```

---

## 第二步：在 Zeabur 部署

1. 打开 https://zeabur.com ，点右上角 **登录**，用 **GitHub 账号**登录（会跳转授权，允许即可）
2. 登录后进入控制台，点 **新建项目**（或 + 号）：
   - 填项目名，比如 `feed-station`
   - **部署区域选「香港」**（或东京/新加坡）——国内朋友访问最快
   - 点 **创建**
3. 进入项目后，点 **添加服务** → 选 **Git**（或「从 GitHub 导入」）：
   - 第一次会要求 **授权 Zeabur 访问你的 GitHub**，允许并选择刚才的 `feed-station` 仓库
   - 选好后 Zeabur 会自动识别为 **Node.js 项目**，点 **部署**（Deploy）
4. 等 1~2 分钟，看到服务状态变成 **RUNNING** 就成功了 🎉

> 如果看到构建日志里有错误，截图发给我，我帮你修。

---

## 第三步：配置持久化（最关键！不然数据会丢）

Zeabur 的服务器重启后，容器里的文件会重置。要让投喂记录和照片不丢，需要把数据目录挂到"硬盘"（持久卷）上：

1. 进入你的服务页面，找到 **「硬盘」**（Volumes）分页
2. 点 **Mount Volumes**，添加**两个卷**：

   | Volume ID（随便起名） | Mount Directory（挂载目录） |
   |---|---|
   | `fs-data` | `/data` |
   | `fs-uploads` | `/uploads` |

   > 挂载目录写 `/data` 和 `/uploads`，和下面的环境变量对应，不依赖项目路径，最稳。
   > 容量选最小档就行（数据很小，每月几毛钱都不到，在 $5 免费额度内）。

3. 回到服务页，找到 **「配置」→「变量」**（环境变量），添加：

   | 变量名 | 值 |
   |---|---|
   | `DATA_DIR` | `/data` |
   | `UPLOAD_DIR` | `/uploads` |

4. 点 **重新部署**（ReDeploy）让配置生效

> ⚠️ 注意：挂载卷时该目录里的旧内容会被清空——没关系，我们本来就是全新环境，空着正好。

---

## 第四步：绑定网址

1. 进入服务页面，点 **「网络」**（Networking / 网域）分页
2. 点 **生成域名**（Generate Domain）
3. 几秒后得到一个网址，类似：`https://feed-station-xxxx.zeabur.app`
4. 点开它——如果看到投喂站登录页，就部署成功啦 🍳

---

## 第五步：发给朋友

把网址复制发给朋友，让他们：

1. 打开网址 → 点「第一次来？开一个自己的投喂站」
2. 注册自己的账号，拿到自己的邀请码
3. 互相输入邀请码加入对方的投喂站 → 开喂！

> 你和朋友**互相加**对方的邀请码，才能互相投喂（你加入 ta 的站，ta 才能喂你；ta 加入你的站，你才能喂 ta）。

---

## 日常使用

| 想做什么 | 怎么做 |
|---|---|
| 改代码后更新网站 | 改完本地文件 → git 提交推送到 GitHub → Zeabur 服务页点 **重新部署** |
| 网站打不开 | 免费版会休眠，第一次打开要等几秒唤醒，正常现象 |
| 备份数据 | 服务页「硬盘」分页可以看到卷，数据在 `db.json` 和图片文件里 |
| 停止网站 | 服务页右上角停止/删除服务（删除后数据在卷里，可以重新挂载） |

---

## 常见问题

**Q：为什么要用环境变量 + 卷，不直接用默认目录？**
A：默认数据目录在项目文件夹里（`/app/data`），服务器重启就没了。挂到 `/data` 和 `/uploads` 这两个独立卷上，重启、重新部署都不丢。

**Q：免费额度够吗？**
A：Zeabur 每月循环送 $5，投喂站这种小流量项目（几十个人用）完全够，卷的存储费也才几毛钱。不用绑信用卡。

**Q：朋友在国外能访问吗？**
A：可以，只是速度看地区。国内选香港节点最优。

**Q：想换一个更好记的域名？**
A：可以绑定自己的域名（需要你在域名商处加一条 CNAME 记录指向 Zeabur 给的地址），免费。

---

有问题随时喊我，我陪着你一步步点 🐾
