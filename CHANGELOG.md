# 投喂站 Changelog

> 这是投喂站的版本管理文档。每次功能迭代、修复、重构，都在**最上方**追加一条版本记录。
>
> **版本号规则：**
> - 修复小问题（bug、文案、样式）→ 第三位 +1，如 `1.0.1`
> - 新增功能 / 交互变化 → 第二位 +1，如 `1.1.0`
> - 架构重构 / 大改版 → 第一位 +1，如 `2.0.0`
>
> **每次更新必填字段：** 版本号、日期、更新内容（用户视角）、技术变更（开发者视角）、变更文件、上线方式。

---

## 1.1.0 — 2026-08-21

### 更新内容（用户视角）

1. 投喂类目重构：8 类新分类——🍉八卦 / 🍬夸夸 / 💌想念 / 🌶️工作吐槽 / 🧊冷笑话 / 📷照片 / 📍此刻我在… / 💬想到啥说啥（「夸奖/赞美」合并为「夸夸」，去掉「链接/文件」）
2. 投喂小票支持下载高清 PNG 图片（3 倍分辨率输出，不再模糊）
3. 「更多」菜单移到页脚，新增「关于 · 更新日志」弹窗；更新日志文案调整为幽默风格
4. 我的饭桌：点日期标签弹出月视图日历，可回看过去日期、与热力图双向同步；选中日期需点「确认」才生效，「回今天」独立为按钮
5. 登录/注册页 footer 同步版本号与完整文案

### 技术变更（开发者视角）

- 前端 `FOODS` 更新为 8 类新分类，`foodOf()` 兼容映射老数据（praise/compliment→kua，link/file→free）
- 小票导出 canvas `SCALE = 3` 高清化，逻辑坐标不变、输出像素 ×3
- 新增日历弹窗 `#calModal`：点日期仅临时高亮，点「确认」才应用；外层「回今天」独立 pill 按钮
- 日历日期用 Date 对象比较，避免 `Y-M-D` 字符串（无前导零）误判未来日期
- `server.js` `VERSION` 升为 `1.1.0`，前端 footer 同步显示
- `test-api.sh` 用例类目 `praise`→`kua`

### 变更文件

- `public/index.html`
- `server.js`
- `test-api.sh`
- `CHANGELOG.md`

### 上线方式

- 本地 commit + tag `v1.1.0` → push GitHub → Railway 自动部署（注意避开高峰时段：中国 14:00-次日 2:00）

---

## 1.0.1 — 2026-08-19

### 更新内容（用户视角）

1. 登录/注册页表单文案调整：「名字（朋友的称呼）」→「你的名字」，「密码」→「你的密码」
2. 页面底部版本号新增个人署名：`v1.0.1 · 来自一只小派`
3. 修复：登录后或切到「我的饭桌」会误弹「🍽️ 收到投喂」提醒——此前只要历史上有过任何投喂记录，重新登录就会白弹一次；现改为只对**真正新增**的投喂提醒

### 技术变更（开发者视角）

- 轮询提醒逻辑重写：从「投喂总数对比」（`lastTableLen`）改为「最新投喂时间戳对比」（`lastSeenMax`，localStorage 键 `fs_lastSeen`）
- 首次轮询只记录基线、不弹提醒；之后仅当饭桌最新投喂 ts 大于已见值时才弹 toast
- `server.js` `VERSION` 常量升为 `1.0.1`，前端 footer 同步显示

### 变更文件

- `public/index.html`
- `server.js`
- `CHANGELOG.md`

### 上线方式

- 本地 commit + tag `v1.0.1` → push GitHub → Railway 自动部署（注意避开高峰时段：中国 14:00-次日 2:00）

---

## 1.0.0 — 2026-08-19

### 一句话定位
**"投喂站"多人版 MVP**：每个人有自己的投喂站，通过邀请码互加为干饭人，把各种内容（八卦/夸奖/赞美/吐槽/冷笑话/照片/链接/文件）做成"食物"投喂给对方；被投喂的内容显示在个人饭桌上，配热度热力图。

### 架构总览

```
浏览器 (public/index.html, 单页应用)
        │  HTTP / JSON / base64 图片
        ▼
server.js（零依赖 Node 后端, 无框架）
        │
        ├── data/db.json      ← 全部结构化数据（JSON 文件）
        └── uploads/          ← 上传的照片（文件）
```

- **后端**：单文件 `server.js`，零 npm 依赖，Node >= 18，端口读 `process.env.PORT`（默认 3789）
- **前端**：单文件 `public/index.html`，原生 HTML/CSS/JS，无构建步骤
- **存储**：`DATA_DIR`（默认 `data/`）+ `UPLOAD_DIR`（默认 `uploads/`），支持环境变量覆盖（云部署挂持久卷用）
- **鉴权**：注册/登录返回随机 token，请求头 `Authorization: Bearer <token>`，服务内存中保存

### 数据模型（data/db.json）

三个顶层数组：

| 集合 | 字段 | 说明 |
|---|---|---|
| `users` | id, name, pass, sp(小动物), inviteCode, token, ts | 注册用户 |
| `memberships` | ownerId(站主), memberId(干饭人), ts | "谁加入了谁的投喂站" |
| `feeds` | id, ownerId(喂的人), memberId(被喂的人), foodId, text, media, reaction(吃后感), ts, dateKey | 投喂记录 |

关系逻辑：A 用 B 的邀请码加入 = 创建 `{ownerId: B, memberId: A}`，此后 **B 可以投喂 A**；互相加入则互喂。撤菜仅允许"喂的人"操作。

### API 清单

| 方法 & 路径 | 鉴权 | 功能 |
|---|---|---|
| POST `/api/register` | 无 | 注册（name/pass/sp），返回用户+token+邀请码 |
| POST `/api/login` | 无 | 登录，返回 token |
| GET `/api/me` | ✅ | 我的信息、邀请码、我的干饭人列表、我加入的站 |
| POST `/api/join` | ✅ | 用邀请码加入某人的投喂站（幂等） |
| POST `/api/feed` | ✅ | 投喂（memberId/foodId/text/mediaUrl），返回带随机吃后感的记录 |
| POST `/api/media` | ✅ | 上传图片（base64 data URL），返回 `/uploads/xxx` 地址 |
| GET `/api/feeds` | ✅ | 我投喂过的记录 |
| GET `/api/table` | ✅ | 我的饭桌：被投喂记录 + 热力图数据（近365天按天统计） |
| DELETE `/api/feed/:id` | ✅ | 撤菜（仅投喂者本人可删） |

### 功能清单（用户视角）

1. **注册/登录**：起名 + 密码 + 选小动物人设，注册即生成 6 位邀请码
2. **邀请码互加**：输入他人邀请码 = 加入其投喂站，成为其干饭人，对方可投喂你
3. **投喂台**：8 种食物（🍉八卦 / 🍬夸奖 / 🍓赞美 / 🌶️吐槽 / 🧊冷笑话 / 📷照片 / 🍔链接 / 🍱文件），配文字，点投喂有飞行动画
4. **吃后感**：被喂方吃掉后随机生成一句反应（真香/有点噎/嗝——等）
5. **我的饭桌**：所有把我加入其站的人喂给我的内容，15 秒自动轮询刷新，收到投喂有提醒
6. **热度热力图**：GitHub 风格近 365 天格子，颜色深浅 = 当日被投喂量；点击格子看当天吃了什么
7. **投喂小票**：任意记录可生成小票文本，一键复制发微信
8. **撤菜**：喂错了可以删除记录（仅投喂者本人）
9. **照片压缩**：前端 Canvas 压缩（最大宽 900px、JPEG 72%）后上传，8MB 上限

### 部署配置（Railway 免费层）

| 项 | 值 |
|---|---|
| 构建方式 | Railpack 自动识别 Node（package.json 有 start 脚本） |
| Root Directory | `feed-station`（GitHub 仓库根目录下套了一层文件夹） |
| 环境变量 | `PORT`（Railway 自动分配）、`DATA_DIR=/data`、`UPLOAD_DIR=/data/uploads` |
| 持久卷 | 1 个：`fs-data` 挂载 `/data`（免费层限 1 卷、总容量 0.5GB） |
| 启动命令 | `node server.js`（npm start） |
| 免费层限制 | 高峰时段（阿姆斯特丹 8:00-20:00 = 中国 14:00-次日 2:00）不能发起新部署；$1/月额度 |

### 验证情况

- `test-api.sh` 全链路 10 项通过（注册→邀请→加入→投喂→饭桌→权限→撤菜→图片上传）
- 线上已部署成功并真实注册使用

### 已知限制 & 待办（v1.1 候选）

- [ ] GIF 动图投喂会变静态图（压缩只取第一帧）
- [ ] 透明底 PNG 转 JPEG 会变白底
- [ ] 无找回密码 / 改名功能
- [ ] 热力图只有"年"视图，无按月切换
- [ ] 头像只有小动物 emoji，无自定义
- [ ] 无管理员数据面板（站长无法在网页查看线上数据）
- [ ] 无消息列表（被投喂提醒只有 toast）
- [ ] 文件投喂（🍱）目前仍走文字路径，未做真实文件上传
